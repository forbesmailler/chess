self_play:
  num_games: 100
  search_depth: 6
  num_threads: 1
  output_file: "training_data.bin"
  max_game_ply: 400
  search_time_ms: 200
  softmax_plies: 10
  softmax_temperature: 200.0
  default_time_control_ms: 60000
  progress_log_interval: 10

training:
  epochs: 100
  batch_size: 4096
  lr: 0.001
  patience: 10
  eval_weight: 0.75
  val_split: 0.1
  random_seed: 42
  max_data_workers: 4

invoke:
  train_games: 50000
  train_threads: 16
  weights: "nnue.bin"

compare:
  num_games: 100
  num_threads: 16
  max_game_ply: 400
  search_time_ms: 200
  progress_log_interval: 10
