self_play:
  num_games: 100
  search_depth: 6
  num_threads: 1
  output_file: "training_data.bin"
  max_game_ply: 400
  search_time_ms: 200
  softmax_plies: 10
  softmax_temperature: 200.0

training:
  epochs: 100
  batch_size: 4096
  lr: 0.001
  patience: 10
  eval_weight: 0.75
  val_split: 0.1

invoke:
  train_games: 10000
  train_threads: 16
  weights: "nnue.bin"

compare:
  num_games: 100
